git remote하는 과정을 간략화하는 방법
- 레포짓을 만들고 로컬 디렉을 만들어 깃 클론을 통해서 바로 연결가능

브런치 개념 다시 이해하기

프로그래밍 언어 : 3형식

저장 조건 반복
대소문자 띄어쓰기 스펠링 주의
변수 [리스트] {딕셔너리}
무엇을 저장하는가
숫자(문자와 결합되지 않은) 글자(``를 넣어서 문자로 취급) 참/거짓(if문에서 주로)

if/else
들여쓰기로 구분하기
else if = elif

while
조건이 참인 동안 반복적으로 실행하므로 종료조건필요
for
정해진 범위를 반복하기에 종료조건이 필요없음

코드 앞에 #이 있으면 그 코드는 실행이 되지 않는다.
여러줄을 블럭하고 싶으면 드래글해서 crtl+/ 

함수와 모듈
내장함수와 그 외

import 
random
random.choice
random.sample

https://www.youtube.com/watch?v=4q5VtIQPINw

숫자 int
문자 str
참거짓 bool

웹 크롤링 
웹 주소 query

import requests.get(주소)